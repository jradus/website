let pallets = {
   first: [ '#444' ],
   second: [ '#444' ]
}

palletChooser = () => {
    let pallet = Math.floor( (Math.random() * 5) + 1 );
    if ( pallet === 1 ) {

    } else if ( pallet === 2 ) {
        
    }
}